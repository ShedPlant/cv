[project]
name = "cv"
version = "0.1.0"
description = "Personal CV Website"
readme = "README.md"
authors = [
    { name = "ShedPlant", email = "ed@shedplant.net" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
cv = "cv:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "zensical>=0.0.57",
]
